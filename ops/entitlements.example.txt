# 구독 권한(entitlement) — 누가 어떤 종목을 볼 수 있는가
#
#   토큰            허용 종목 (쉼표로 구분, * 는 전체)
#
# 이 파일을 MDFEED_ENTITLEMENTS_FILE 로 지정하면 배포 게이트웨이가 검사한다.
# 지정하지 않으면 검사가 꺼지고, 그 사실이 기동 로그에 WARNING 으로 남는다.
#
# 막는 것   : 권한 없는 구독자가 종목을 받아 가는 것. 요청해도, 필터를 비워도 못 받는다.
# 못 막는 것: 도청. 토큰과 시세가 평문으로 흐른다. 전송 구간 보호는 사설망이나
#             TLS 종단이 맡는다(RUNBOOK). 토큰을 비밀번호처럼 다루되 암호로 착각하지 않는다.
#
# 파일을 바꾼 뒤에는 게이트웨이를 무중단 교체한다: ops/swap_gateway.sh

demo-readonly    UPBIT:KRW-BTC,BINANCE:BTCUSDT
krx-desk         KIS:005930,KIS:000660
full-desk        *
