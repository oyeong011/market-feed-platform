# 상세 — 계층별 설계와 실측

[README](README.md) 가 진입점이고, 이 문서는 각 계층을 자세히 다룹니다.
설계 판단의 근거는 [DESIGN.md](DESIGN.md), 장애 대응은 [RUNBOOK.md](RUNBOOK.md) 입니다.

---

## 자산군 — 주식만으로는 피드가 아닙니다

마켓데이터 서비스에서 주식만 다루는 경우는 없습니다. 금리와 지수는 다른 모든 자산의
기준선이고, 국내 금융정보 서비스의 주력이기도 합니다.

| 자산군 | 경로 | 대상 | 갱신 |
|---|---|---|---|
| 암호화폐 | 업비트·바이낸스 WebSocket | 7 | 실시간 틱 |
| 국내주식 (틱) | KIS WebSocket `H0STCNT0` | 3 | 실시간 체결 |
| 국내주식 (전체) | KIS REST 계층형 | **1,783** | 15분 회전 |
| **지수** | KIS 업종 현재지수 | 6 (코스피·코스피200·코스닥·대·중·소형주) | 10초 |
| **금리** | KIS 금리 종합 | 7 (미국채 30·10·1년, 연방기금, 재할인, 단기우대, 일본 10년) | 5분 |

**휴장일 캘린더**도 KIS 에서 받아 둡니다. 이전에는 요일과 시각만 보고 장 시간을 판정해
공휴일에 "데이터가 안 온다"를 장애와 구분하지 못했습니다.

### 국내 금리를 발행하지 않는 이유

`comp-interest` 의 국내 금리 응답(`output2`)은 **두 가지가 동시에 깨져 있습니다.**

1. **키와 값의 대응이 행마다 어긋납니다.** `prdy_vrss_sign`(전일대비부호) 자리에
   종목명 `국고채 30년` 이, 전일대비 자리에 금리 `4.5950` 이 들어 있습니다.
2. **일부 행은 종목명이 이미 손상된 채로 옵니다.** 바이트를 직접 확인하니
   `\xef\xbf\xbd`(U+FFFD 대체문자)였습니다. 우리 쪽 디코딩 문제가 아니라
   **서버가 보내기 전에 원본을 잃은 것**이라 복원할 방법이 없습니다.

값의 모양으로 짝지어 봤지만 그 짝짓기도 믿을 수 없었습니다 —
`CD AAA 3개월(13주)` 에 2.0000 이 붙는데 `CD 91일` 은 3.12 입니다. 하나는 틀렸습니다.

그래서 **국내 금리는 발행하지 않습니다.** 이 프로젝트가 세운 원칙 중 하나가
*"틀린 값을 조용히 배포하는 것이 값이 없는 것보다 나쁘다"* 이고, 여기가 정확히
그 경우입니다. 그럴듯한 숫자를 내보내는 대신 **안 내보내고, 그 사실과 이유를
`/healthz` 에 남깁니다.**

```json
"domestic_rates_published": false,
"domestic_rates_reason": "응답의 키·값 대응이 어긋나고 일부 종목명이 서버 쪽에서
                          U+FFFD 로 손상돼 온다. 짝짓기를 신뢰할 수 없어 발행하지 않는다.",
"domestic_rate_rows_seen": 54, "domestic_rate_rows_corrupt": 24
```

손상 건수는 계속 셉니다. KIS 가 응답 형식을 고치면 그 카운터가 먼저 알려줍니다.

---

## 국내 주식을 넓게 덮는 방법 — 계층형 피드

KIS 실시간 웹소켓은 **계정당 등록 한도**가 있습니다. 이 계정은 실측 결과 **3종목**이었습니다
(새 연결에서 한 종목씩 등록해도 4번째부터 `MAX SUBSCRIBE OVER`. 문서 기준값 41과 다르고
계정마다 다릅니다). 코스피 전체를 틱 단위로 받으려면 거래소와의 정식 시세 계약이 필요합니다.

그래서 실제 마켓데이터 시스템이 쓰는 **계층형 피드**로 우회했습니다.

| 계층 | 방식 | 종목 수 | 갱신 | 데이터 성격 |
|---|---|---:|---|---|
| 1 · 틱 | WebSocket `H0STCNT0` | 3 | 밀리초 | **실제 체결** (가격·수량·방향·호가잔량) |
| 2 · 활성 | 순위 API (30종목/요청) | ~60 | 10초 | 스냅샷 |
| 3 · 전체 | 유니버스 라운드로빈 | **1,783** | 약 15분 회전 | 스냅샷 |

2계층이 효율의 핵심입니다. 순위 API는 **한 번 호출에 30종목**을 주므로 같은 요청 예산으로
30배 넓게 덮고, 거래가 활발한 종목이 자동으로 상위에 올라 "지금 움직이는 종목"이
우선 갱신됩니다.

**스냅샷을 체결로 위조하지 않습니다.** REST 응답은 체결이 아니라 그 순간의 상태입니다.
직전 폴링 대비 **누적거래량이 늘어난 만큼만** 합성 체결로 발행하고, venue를 `KRX`로 따로 둬
웹소켓에서 온 진짜 체결(`KIS`)과 섞이지 않게 했습니다. 지연 지표에서도 제외합니다 —
폴링 주기가 곧 지연이라 네트워크 지연을 재는 의미가 없습니다.

**유량 제한은 스스로 찾아갑니다.** 실측 한도는 초당 5건이었고 4건에서도 실패가 났습니다.
고정 3건/초로 두니 60초에 15번 거절당했습니다. TCP 혼잡제어와 같은 방식(AIMD)으로
바꿔 — 거절하면 간격 1.15배, 8회 연속 성공하면 0.9배 — **2.0 req/s로 수렴**했습니다.
거절 한 번의 비용은 요청 슬롯 하나뿐이라, 거절을 피하는 것보다 성공 요청 수를
최대화하는 쪽이 맞습니다.

```bash
python scripts/fetch_krx_symbols.py      # 종목 마스터 갱신 (KOSPI 1,783 + KOSDAQ 1,771)
MDFEED_ADAPTERS=upbit,binance,kis,kis_rest make up
```

---

## 데이터 품질 — 원칙을 집행하는 부분

이 문서 맨 위에 *"틀린 값을 조용히 배포한다"* 를 네 가지 문제 중 하나로 적어 뒀습니다.
그런데 **그걸 탐지하는 장치가 없었습니다.** 원칙만 있고 집행이 없으면 원칙이 아닙니다.

`quality` 프로세스가 버스를 구독해 계속 검사합니다.

| 검사 | 무엇이 틀렸다는 신호인가 |
|---|---|
| **가격 점프** | 파싱 오프셋이 밀렸거나 소수점을 잘못 읽었다 |
| **크로스된 호가** | 매수 > 매도. 정상 시장에서 지속 불가 — 필드가 뒤바뀐 신호 |
| **광폭 스프레드** | 유동성 고갈이거나 한쪽 호가만 갱신 중 |
| **정체** | 같은 **기록**(가격+체결시각)이 계속 온다. 연결은 살아 있는데 상류가 같은 레코드를 반복한다. 가격만 같고 시각이 전진하면 그냥 조용한 시장이라 알리지 않는다 — 옛 판정은 이걸 구분 못 해 실측 400,000건에서 117건이 전부 오탐이었다 |
| **OHLC 위반** | 집계 로직 버그. 하류가 전부 오염된다 |
| **교차 시장 괴리** | 같은 자산이 두 거래소에서 벌어짐 |

### 오탐을 줄이는 방법 — 종목마다 정상 범위가 다르다

"X% 넘으면 이상"으로만 하면 급등락에서 오탐이 쏟아지고, **사람이 알람을 무시하게
됩니다. 그러면 진짜가 왔을 때도 무시합니다.**

그래서 절대 임계와 함께 **최근 변동성 대비**를 봅니다. 평소 0.01% 씩 움직이던
종목의 1% 는 이상하지만, 평소 3% 씩 흔들리던 종목의 2% 는 그냥 변동성입니다.
(중앙값을 씁니다 — 평균은 이상치에 끌려갑니다)

### 외부 소스 없이 자체 정합성 검사 — 암묵 환율

업비트는 원화, 바이낸스는 달러로 같은 코인을 거래합니다.
**두 가격의 비율이 곧 환율**이고, 여러 코인에서 뽑은 값은 서로 가까워야 합니다.

실측 (2026-08-27):

```
BTC 1,385.2   ETH 1,386.2   SOL 1,384.0   XRP 1,386.2   KRW/USD
```

4개 자산에서 **독립적으로** 뽑았는데 **0.16% 이내로 수렴**합니다.
외부 환율 소스 없이 데이터 일관성을 증명한 것입니다.
하나만 벌어지면 그 종목의 시세가 이상하다는 뜻이고, 어느 쪽인지는 나머지가 알려줍니다.

부수적으로 이 값 자체가 상품이 됩니다 — 국내 시장 프리미엄 지표입니다.

### 이상을 발견해도 데이터를 버리지 않습니다

마켓데이터에서 "이상해 보이는 값"의 상당수는 **진짜 시장 움직임**입니다.
급등락, 유동성 고갈, 서킷브레이커. 자동으로 걸러내면 정작 중요한 순간의 데이터를 잃습니다.

**표시하고 기록할 뿐, 판단은 사람이 합니다.** 마켓데이터 벤더가 `suspect` 플래그를
붙여 내보내되 지우지는 않는 관행과 같습니다.

검사기를 별도 프로세스로 둔 이유도 같습니다 — 검사는 **관찰자**여야 합니다.
수집기에 붙이면 hot path 가 무거워지고, 검사 로직 버그가 수집까지 죽입니다.

```bash
curl localhost:9106/healthz     # 검사 건수·이상 통계
curl localhost:9106/events      # 최근 탐지 목록
curl localhost:9106/fx          # 암묵 환율
```

---

## 수평 확장 — 단일 장애점 제거

`feedd` 하나가 모든 업스트림을 들면 거래소 하나의 문제로 전부가 내려갑니다.
venue 그룹별로 샤드를 쪼갭니다.

```bash
make up-shards      # feedd:crypto + feedd:krx 로 분리 기동
```

```
feedd:crypto  bus-crypto.sock  :9100   업비트·바이낸스
feedd:krx     bus-krx.sock     :9200   KIS 틱·KRX 광역·지수/금리
      └──────────┬──────────┘
                 └─▶ 소비자 5개가 MDFEED_BUS_PATHS 로 양쪽 모두 구독
```

**실측 검증** — `krx` 샤드를 `SIGKILL` 한 뒤:

| | 결과 |
|---|---|
| `crypto` 샤드 | 그대로 `healthy`, 구독자 4명 유지 |
| 소비자 3개 | **PID 유지**, 계속 수신 |
| 적재 | 4,533 → 6,978행 (**2,445행 증가**) |
| `krx` 샤드 | 감독기가 재시작해 복귀 |

샤드마다 seq 공간이 독립이므로 `writer` 는 **소스별로 시퀀스를 따로 추적**합니다.
하나로 추적하면 샤드가 바뀔 때마다 거짓 갭이 잡힙니다.

---

## 부하 — 어디서 무너지나

`bench/load_test.py` 로 구독자를 늘려 가며 쟀습니다. 소스 속도를 고정하려고
리플레이를 3배속으로 돌렸습니다 — 실거래소는 회차마다 틱 밀도가 달라
구독자 수에 따른 변화인지 구분할 수 없습니다.

| 구독자 | 총 처리량 | 1인당 유지율 | p99 지연 | 유실 | 드롭 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 40 msg/s | 100% | 1.4 ms | 0 | 0 |
| 10 | 491 | 124% | 3.7 ms | 0 | 0 |
| 25 | 1,330 | 139% | 8.0 ms | 0 | 0 |
| 50 | 2,507 | 130% | 12.8 ms | 0 | 0 |
| 100 | 6,376 | 168% | **27.9 ms** | 0 | 0 |

**처리량은 버팁니다.** 1인당 처리량이 유지되므로 팬아웃은 선형입니다.
총 처리량이 160배 늘어도 유실·드롭이 0입니다.
**무너지는 건 지연입니다** — p99 가 19배.

### 이 측정이 제 가설을 뒤집었습니다

설계 문서에 *"구독자가 수백 명이면 재인코딩이 병목"* 이라고 적어 뒀습니다.
**몇 명인지 재보지도 않고 쓴 문장이었습니다.**

재보니 구독자 100명·6,376 msg/s 에서 재인코딩이 쓰는 CPU 는 **0.28%** 입니다.
팬아웃 단계를 쪼개니 구독자당 비용이 이렇습니다.

| 단계 | 구독자당 | 비중 |
| --- | ---: | ---: |
| 구독자별 재인코딩 | 552 ns | 23% |
| 큐 삽입 | 410 ns | 18% |
| **소켓 write + drain** | **1,476 ns** | **59%** |

진짜 비용은 **소켓 쓰기**입니다. 재인코딩을 없애도 전체의 4분의 1만 줄어듭니다.
제가 계획했던 채널 기반 시퀀싱은 이 문제를 거의 풀지 못합니다.

> **근거 없이 적은 한계 서술은 없느니만 못합니다.** 그럴듯해서 아무도 의심하지 않고,
> 그 위에 잘못된 최적화 계획이 쌓입니다.

---

## 관측 — 지표 · 알람 · 누수 추적

```bash
docker compose -f docker-compose.observability.yml up -d
#   Prometheus  http://localhost:9090
#   Grafana     http://localhost:3000   대시보드 12패널 자동 프로비저닝
```

8개 프로세스가 각자 `/metrics` 를 냅니다. 하나로 합치지 않은 이유는
**어느 프로세스가 아픈지 구분하기 위해서**입니다. 샤드도 따로 잡습니다 —
`feedd:crypto` 와 `feedd:krx` 는 다른 이유로 아플 수 있습니다.

### 알람이 실재하는 지표를 참조하는지 검증합니다

**존재하지 않는 지표를 참조하는 알람은 영원히 울리지 않습니다.**
그런데 Prometheus 는 아무 오류도 내지 않습니다 — 그냥 조용히 no data 입니다.
설정은 문법적으로 완벽하고, 대시보드에 규칙도 보이고, 아무도 이상을 못 느낍니다.

```bash
python scripts/verify_alerts.py
```

처음 돌렸을 때 **11개 중 4개가 안 울릴 알람**이었습니다.

| 지표 | 왜 없었나 |
|---|---|
| `dropped_total`, `gap_messages_total` | **카운터를 0으로 초기화하지 않음.** 첫 사건이 나야 지표가 생기므로 `increase()` 가 평가되지 않음 |
| `rss_growth`, `fd_growth` | 표본이 모이기 전에는 키 자체가 없었음 |

**검증기 자체도 CI 에서 한 번 틀렸습니다.** 리플레이 전용 구성에서
`ingest_latency`·`clock_offset` 이 없다고 실패했는데, 리플레이는 지연을 측정하지
않으므로 **없는 것이 정확한 동작**이었습니다. 없는 값을 0 으로 채우면
"지연이 0µs" 라는 거짓말이 됩니다.

검증기를 약화시키는 대신 **조건을 이해하게** 고쳤습니다 — feedd 에 물어
지연 측정 어댑터가 하나라도 있는지 확인하고, 있는데도 지표가 없으면 여전히 실패합니다.

드롭이 한 번도 없는 것과 드롭 지표가 없는 것은 다릅니다. 전자는 좋은 소식이고
후자는 계측이 안 되고 있다는 뜻인데, 초기화를 안 하면 **둘이 구분되지 않습니다.**

### 알람이 진짜 문제 3개를 잡았습니다

| 대기 알람 | 실제 값 | 판정 |
|---|---|---|
| `FeedThroughputZero` (krx) | 0 msg/s | **알람이 장 시간을 몰랐습니다.** 밤마다 울리면 사람이 무시합니다 → `market_open` 지표를 노출하고 조건에 반영 |
| `FdLeak` / `MemoryLeak` | fd +16/h | **기동 직후 노이즈.** 3분 표본을 시간 단위로 외삽한 값 → 15분 미만이면 0 을 보고 |
| `ClockSkewHigh` | **−2,046,610 µs** | **진짜입니다.** 로컬 시계가 2초 뒤처져 있었습니다 |

앞의 둘은 고쳤고, 셋째는 알람이 제 역할을 한 것입니다.

### 누수 추적

각 프로세스가 자기 RSS·fd·스레드를 보고합니다. **누수는 절대값이 아니라 기울기로**
봅니다 — RSS 200MB 가 문제인지는 그게 자라고 있는지에 달렸습니다.

```bash
make soak MINUTES=60      # 장시간 감시 → 임계 초과 시 종료코드 1
```

| 항목 | 임계 |
|---|---|
| RSS 증가 | 5 MB/h (하루 120MB, 한 주면 컨테이너 한도) |
| fd 증가 | 1개/h **그리고** 절대 +2 이상 |
| 처리량 유지 | 시작 대비 50% |

> **여기서도 도구가 거짓 양성을 냈습니다.** 4분 관측에서 `feedd-krx fd +4.4/h` 가
> 걸렸는데 시작도 21개, 끝도 21개였습니다. 중간에 한 번 튄 걸 시간 단위로
> 외삽한 결과였습니다. 20분 미만 관측에서는 기울기로 판정하지 않고,
> **관측이 짧다는 사실을 결과에 남깁니다.**

---

## 장애 주입 — 복구 경로는 평소에 안 돌아갑니다

이 프로젝트에는 복구 장치가 여럿 있습니다. 지수 백오프 재접속, CRC 재동기화,
느린 구독자 격리, DB 폴백, 샤드 격리. 그런데 **평소에는 하나도 안 돌아갑니다.**
61분을 돌려도 재접속 0회였습니다. 즉 그 코드가 맞는지 확인된 적이 없었습니다.

**안 돌아본 복구 경로는 복구 경로가 아닙니다.** 그래서 일부러 부숩니다.

```bash
./ops/chaos.sh all
```

| 시나리오 | 결과 |
|---|---|
| TCP 스트림에 쓰레기 주입 | 오염 3회 → **재동기화 4회 후 1,101건 계속 수신** |
| 읽지 않는 구독자 25초 | 게이트웨이 정상 유지 |
| DB 파일 권한 박탈 | writer 생존 · 복구 후 적재 재개 (32,429 → 35,271) |
| `feedd` SIGKILL | writer **PID 유지** + 수신 재개 (38,047 → 39,975) |

> **첫 판에서 CRC 시나리오가 "재동기화 0회" 로 통과했습니다.** 확인해 보니
> 주입 조건이 안 맞아 오염을 거의 안 넣은 것이었습니다 — 실패가 아니라
> **테스트가 돌지 않은 것**입니다. 그런 결과를 통과로 세면 검증 자체가
> 거짓말이 됩니다. 주입 횟수를 판정 조건에 넣어 "안 돌았으면 실패" 가 되게 고쳤습니다.

---

## 퀀트 — 백테스트는 오픈소스에 맡깁니다

`backtesting.py` 가 있는데 백테스트 엔진을 새로 만들 이유는 없습니다.
**성과 판정의 기준은 오픈소스**이고, 우리가 제공하는 건 데이터와 전략 코드입니다.

그럼 `quant/backtest.py` 는 왜 있나 — **엔진이 아니라 대조용 실행기**입니다.

`src/mdfeed/strategies.py` 의 전략 클래스는 **실시간 엔진이 그대로 쓰는 코드**입니다.
그 코드를 오픈소스 백테스터에 넣으려면 상대 API 로 다시 써야 하는데, 그 순간
"실시간과 같은 코드"라는 보장이 깨집니다. 그래서 실시간 클래스를 과거 봉에 그대로
먹이는 최소 실행기를 두고, **두 경로의 결과를 대조**합니다.

```
기준   backtesting.py     성과 수치는 이쪽을 인용
대조   자체 최소 실행기    실시간 전략 코드가 같은 신호를 내는지 확인
```

### 대조 결과

동일 조건(슬리피지 0 · 수수료만 · 자본 95% · 주문 단위 1주)으로 맞춘 뒤:

| 항목 | 결과 |
|---|---|
| SMA(30) 값 | 최대 절대오차 **0.00e+00** |
| **SMA 교차 시점** | pandas 와 **9곳 완전 일치** |
| RSI(14) | Wilder 공식 예제와 소수 둘째 자리까지 일치 |
| 총수익률 | 0.27%p 차이 (거래 5회 vs 4회 — 주문 모델 차이) |

**이 대조가 우리 쪽 버그를 하나 찾아냈습니다.** 삼성전자 10초봉에서 교차 지점이
pandas 와 3곳 어긋났는데, 원인은 두 이동평균이 **정확히 같아지는 순간**을
"아래로 내려감"으로 오인한 것이었습니다. 국내 주식은 호가 단위가 커서 실제로
일어나지만, 크립토 float 에서는 거의 없어 오래 드러나지 않았습니다.

> **수익률만 비교했으면 못 찾았습니다.** 처음엔 0.43%p 차이를 보고 "체결 규칙 차이"로
> 넘겼습니다. **교차 시점 자체를 대조**하고 나서야 지표 계산 쪽 문제라는 게 드러났습니다.

### 정직성 장치

- **미래 참조 차단** — `t` 봉 종가로 판단, **`t+1` 봉 시가**로 체결
- **수수료 + 슬리피지** — 편도 0.05% + 5bp
- **주문 단위** — 주식은 1주, 크립토는 소수점 (`lot_size_for`)
- **미청산 포지션 강제 청산** — 남기면 최종 자산이 부풀려집니다

```bash
make backtest    # → docs/data/backtest.json (대시보드가 읽음)
```

**결과에 대한 경계**: 저장소 샘플은 38분 구간 단일 종목입니다. 통계적 유의성이 없습니다.
이 수치는 **파이프라인이 끝까지 동작한다는 증거**이지 전략의 우열이 아닙니다.
실제로 세 전략 모두 손실이었고, 그대로 적었습니다.

---

## 데이터 평면 두 개

```
실시간 평면  거래소 WS ─▶ feedd ─▶ 1분봉        밀리초 단위 · 휘발성
참조 평면    SEC EDGAR / OpenDART ─▶ 재무제표    분기 단위 · 영속 (487,434건)
                                    └─ oyeong011/financial-database
```

조인 키는 **`DART stock_code` = `KIS 종목코드`** 입니다. 삼성전자는 참조 평면에서 `005930`,
실시간 평면에서 `KIS:005930` 입니다. 크립토 심볼은 대응하는 재무제표가 없으므로
**억지로 붙이지 않습니다**.

```bash
make factor    # DART 재무 팩터 스크리닝 (영업이익률·ROE·매출성장·FCF·부채비율)
```

결측을 0으로 채우지 않습니다. 채우는 순간 "부채가 없는 회사"와 "부채 데이터가 없는 회사"가
같아져 스크리닝 결과가 조용히 오염됩니다.

---

---

## 운영

### 실제 리눅스 systemd 에서 검증했습니다

유닛 파일을 "썼다"와 "돌려봤다"는 다릅니다. 개발은 macOS 에서 했으므로
systemd 가 실제로 도는 Ubuntu 22.04 컨테이너를 만들어 확인했습니다.

```bash
make systemd-test        # systemd 테스트베드에 설치·기동·검증
```

확인한 것:

| 항목 | 결과 |
|---|---|
| 6개 서비스 + target 기동 | 전부 `active (running)`, `User=mdfeed` |
| 보안 강화 | `ProtectSystem=strict` · `ProtectHome=yes` · `NoNewPrivileges=yes` · `LimitNOFILE=65536` 적용 확인 |
| 구조화 로그 | JSON 한 줄씩 journald 로 수집 |
| **SIGKILL 후 재시작** | feedd PID 133 → 253, `Restart=on-failure` 동작 |
| **소비자 생존** | feedd 재시작 시 소비자 3개 PID 유지, 백오프 1s→2s→4s 후 자동 재접속 |
| **우아한 종료** | `systemctl stop` 즉시 완료, 종료 flush 로 잔여 행 저장, 프로세스 잔류 없음 |

```bash
# 리눅스 서버 배포
sudo make install-systemd
systemctl start mdfeed.target

# 점검
./ops/ops.sh status     # 프로세스 + HTTP 헬스 + 포트를 한 화면에
./ops/ops.sh diag       # 업스트림·시퀀스 무결성·시계 동기화·디스크까지
./ops/ops.sh top        # 자원 사용량 + 파일 디스크립터
./ops/ops.sh conns      # 현재 구독자 연결
python3 ops/healthcheck.py    # 외부 감시용 (종료코드 0/1/2)
```

**왜 워치독이 따로 필요한가.** systemd의 `Restart=on-failure` 는 *프로세스가 죽어야* 동작합니다.
그런데 피드 서비스의 실제 장애는 대부분 프로세스가 멀쩡한 채로 옵니다 —
TCP half-open(소켓은 ESTABLISHED인데 데이터가 없음), 거래소의 조용한 구독 해지,
이벤트 루프가 블로킹 호출에 물림. 전부 `/healthz` 로만 탐지되므로
**헬스 판정 → 재시작** 경로를 따로 뒀습니다. 시간당 재시작 한도를 넘으면
재시작으로 못 고치는 문제로 보고 사람을 호출합니다.

모든 서비스가 동일한 점검 규약을 갖습니다: `/healthz`(liveness) · `/readyz`(readiness) ·
`/metrics`(Prometheus). liveness와 readiness를 나눈 이유는, 기동 직후 DB 연결 전 상태를
"죽었다"고 판정해 재시작하면 영원히 못 뜨기 때문입니다.

---

---

## 자격증명

한국투자증권 어댑터는 앱키가 필요합니다. **저장소에 넣지 마세요.**

```bash
cat > ~/.mdfeed/kis.env <<'EOF'
KIS_APP_KEY="..."
KIS_APP_SECRET="..."
KIS_ENV="real"          # real 실전 / vts 모의
EOF
chmod 600 ~/.mdfeed/kis.env

MDFEED_ENV_FILE=~/.mdfeed/kis.env MDFEED_ADAPTERS=upbit,binance,kis make up
```

`MDFEED_ENV_FILE` 은 docker 의 `--env-file`, systemd 의 `EnvironmentFile=` 과 같은 역할입니다.
이미 설정된 환경변수는 덮어쓰지 않고, 값은 어디에도 로그로 남기지 않습니다.
**키가 없으면 KIS 어댑터만 스스로 비활성화되고 나머지는 정상 기동합니다.**

---
