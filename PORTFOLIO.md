# 채용 요건 대응 — STAR

연합인포맥스 채용 요건 각 항목을 **Situation · Task · Action · Result · Learning** 으로
정리했습니다. 모든 수치는 실측이며, 확인 위치를 함께 적었습니다.

- 저장소 <https://github.com/oyeong011/market-feed-platform>
- 데모 <https://oyeong011.github.io/market-feed-platform/>

> **소유권 표기** — 이 프로젝트의 구현에는 AI 를 상당 부분 활용했습니다.
> 아래에서 **판단·측정·결정**은 본인 것이고, 코드 작성은 AI 보조입니다.
> 각 항목의 `Action` 에서 그 경계를 구분해 적었습니다.

---

---

# 프로젝트 전체 — 하나의 STAR

## S · 상황

금융정보 서비스는 거래소에서 시세를 받아 고객 단말에 공급합니다. 그런데 거래소마다
형식이 전부 다릅니다.

```
업비트     JSON 을 바이너리 프레임으로 보냄
바이낸스   가격을 문자열로 보냄
한국투자   005930^103546^266250^…  파이프·캐럿 구분 문자열
```

이걸 하나로 통일해 여러 소비자에게 나눠주는 중간 계층이 **FEED 서비스**입니다.
연합인포맥스가 하는 일이 이것이고, 채용 요건의 대부분이 이 계층에서 나옵니다 —
프로세스 간 통신, TCP/HTTP, 리눅스 서비스 점검, 파이프라인 자동화.

**요건에 "안다" 고 쓰는 대신 동작하는 것으로 보이기로 했습니다.**

## T · 과제

기능을 늘리는 것이 아니라 **네 가지 실패를 막는 것**을 목표로 정의했습니다.
이 정의가 이후 모든 판단의 기준이 됐습니다.

| # | 문제 | 왜 어려운가 |
|---|---|---|
| 1 | 세션은 반드시 끊긴다 | 끊긴 구간은 영구 유실. 거래소는 과거를 다시 주지 않는다 |
| 2 | 느린 소비자 하나가 전체를 멈춘다 | 한 구독자를 기다리면 전원이 굶는다 |
| 3 | 프로세스는 멀쩡한데 데이터만 안 온다 | 실제 장애의 대부분. `systemctl status` 로 안 잡힌다 |
| 4 | 틀린 값을 조용히 배포한다 | 값이 없는 것보다 나쁘다. 아무 알람도 안 울린다 |

그리고 **어디까지 직접 만들지 경계**를 먼저 정했습니다.

| 계층 | 선택 | 이유 |
|---|---|---|
| 피드 (프로토콜·프레이밍·갭 복구) | **직접** | 라이브러리를 써도 프레이밍·시퀀싱·갭 복구는 어차피 직접 해야 한다 |
| 분석 (백테스트) | **오픈소스** | `backtesting.py` 가 낫다. 새로 만들 이유가 없다 |
| 저장소 | **오픈소스** | PostgreSQL + TimescaleDB |

## A · 행동

**① 구조** — 수집을 한 프로세스로 모으고 배포·적재·전략·품질검사를 분리했습니다.
소비자가 각자 거래소에 붙으면 같은 데이터를 N 번 받고 N 배로 끊깁니다.

```
거래소 ─▶ feedd:crypto ─┐            ┌─▶ tcp-gateway  MDFP/1 바이너리
         feedd:krx    ─┴─ UDS 버스 ─┼─▶ ws-gateway   WebSocket + 대시보드
         (샤딩)                     ├─▶ writer       1분봉 · PostgreSQL
                                    ├─▶ rest-api     과거 조회
                                    ├─▶ strategy     지표 → SIGNAL
                                    └─▶ quality      품질 검사
```

**② 문제 1(세션 단절) 대응** — 지수 백오프 + 지터 재접속, 정체 감지, 스냅샷+증분 복구,
시퀀스 갭 탐지. 샤딩으로 거래소 하나의 장애가 전체를 내리지 않게 했습니다.

**③ 문제 2(느린 소비자) 대응** — 구독자별 유한 큐 + drop-oldest. 발행 경로에서
`drain()` 을 기다리면 구독자 하나가 느릴 때 전체가 멈춥니다(head-of-line blocking).
유실은 허용하되 반드시 셉니다.

**④ 문제 3(무데이터 장애) 대응** — liveness/readiness 분리, 헬스 기반 워치독,
`market_open` 지표. systemd 의 `Restart=on-failure` 는 프로세스가 죽어야 동작하는데
피드 장애는 대부분 프로세스가 멀쩡한 채로 옵니다.

**⑤ 문제 4(조용한 오류) 대응** — 이게 가장 오래 걸렸습니다. 원칙은 처음부터 적어
뒀는데 **집행 장치가 없었습니다.** 원칙만 있고 집행이 없으면 원칙이 아닙니다.
품질 검사 프로세스를 별도로 두고 6가지를 계속 검사하게 했습니다.

**⑥ 그리고 계속 측정했습니다** — 참조 구독 클라이언트, 부하 시험, soak, 장애 주입,
알람 검증. **측정하지 않았으면 몰랐을 것들이 계속 나왔습니다.**

## R · 결과

| 축 | 결과 |
|---|---|
| **커버리지** | 수집 경로 5개 · 자산군 4축 · 코스피 **1,783종목** |
| **무결성** | 구독자 데이터 무결성 **48.5% → 100.0000%** |
| **성능** | IPC 지연 **p50 47.3 µs** · 파이프라인 49,235 msg/s · 적재 480,586 rows/s |
| **안정성** | systemd 11항목 자동 검증 · 장애 주입 4종 복구 확인 |
| **품질** | 108,156건 검사 → CRITICAL 0 · 암묵 환율 자산 간 괴리 **0.050%** |
| **자동화** | CI 6잡 · 테스트 **179개** · 대시보드 지표 자동 갱신 |
| **기록** | 실측으로 잡은 결함 **26건**, 원인·조치 전부 문서화 |

## I · 배운 것

**① 문서를 믿기 전에 한 번 재본다.**
KIS 문서는 체결구분 `3` 이 매도라고 했지만 실제로는 `5` 였습니다. 300건을 호가와
대조해 확인했습니다. 문서대로 뒀으면 매도 체결이 전량 미상으로 남아 주문 흐름
지표가 통째로 무의미해졌을 것입니다.

**② 근거 없이 적은 한계 서술은 없느니만 못하다.**
"구독자 수백 명이면 재인코딩이 병목" 이라고 적어 뒀는데, 재보니 CPU 0.28% 였습니다.
진짜 비용은 소켓 쓰기(59%)였습니다. 그럴듯한 문장은 아무도 의심하지 않고,
그 위에 잘못된 최적화 계획이 쌓입니다.

**③ 안 돌아본 복구 경로는 복구 경로가 아니다.**
61분을 돌려도 재접속이 0회였습니다. 복구 코드가 맞는지 확인된 적이 없었다는 뜻입니다.
장애를 일부러 주입하고 나서야 `Requires=` 한 줄이 자동 재접속 설계를 무력화하고
있다는 것을 알았습니다.

**④ 계측 도구도 거짓말을 한다.**
알람 11개 중 4개가 영원히 안 울릴 상태였습니다(카운터 0 초기화 누락).
누수 알람은 기동 때마다 오탐이 났습니다(짧은 표본 외삽).
장애 주입 테스트 하나는 **아예 안 돌고 통과**했습니다.
계측을 계측해야 합니다.

**⑤ 값이 없는 것보다 틀린 값이 나쁘다.**
국내 금리 응답이 서버 쪽에서 손상돼 왔을 때, 그럴듯한 숫자를 내보내는 대신
**발행하지 않고 그 이유를 노출**하기로 했습니다. 금리 7종을 더 보여주는 것보다,
그중 하나가 틀렸을 때 그걸 근거로 내린 판단이 틀어지는 쪽이 훨씬 비쌉니다.

---

# 요건별 STAR

## 필수 1. 금융 데이터 FEED 서비스 개발 및 운영

**S** — 거래소마다 시세 형식이 전부 다릅니다. 업비트는 JSON 을 바이너리 프레임으로,
바이낸스는 가격을 문자열로, 한국투자증권은 `005930^103546^266250^…` 파이프·캐럿
구분 문자열로 보냅니다. 이걸 하나로 통일해 여러 소비자에게 나눠주는 중간 계층이
곧 FEED 서비스입니다.

**T** — 기능을 늘리는 것이 아니라 **네 가지 실패**를 막는 것을 목표로 잡았습니다.

| 문제 | 왜 어려운가 |
|---|---|
| 세션은 반드시 끊긴다 | 끊긴 구간은 영구 유실. 거래소는 과거를 다시 주지 않는다 |
| 느린 소비자 하나가 전체를 멈춘다 | 한 구독자를 기다리면 전원이 굶는다 |
| 프로세스는 멀쩡한데 데이터만 안 온다 | 실제 장애의 대부분. `systemctl status` 로 안 잡힌다 |
| 틀린 값을 조용히 배포한다 | 값이 없는 것보다 나쁘다. 아무 알람도 안 울린다 |

**A**
- *(판단)* 수집을 한 프로세스로 모으고 배포·적재·전략을 분리했습니다. 소비자가
  각자 거래소에 붙으면 같은 데이터를 N 번 받고 N 배로 끊깁니다.
- *(판단)* 스냅샷 + 증분 구조를 넣었습니다. 새 구독자가 다음 체결까지 빈 화면을
  보지 않도록 접속 즉시 최신값을 보내고 증분을 잇습니다.
- *(측정)* 참조 구독 클라이언트를 만들어 무결성을 실측했습니다. **48.5% 가 나왔습니다.**
  원인은 게이트웨이가 심볼 필터링을 하는데 시퀀스는 전역 번호를 그대로 흘린 것이었습니다.
  구독자 입장에서 걸러진 번호가 전부 유실로 보였습니다.
- *(결정)* 구독자별로 시퀀스를 다시 매기게 바꿨습니다.

**R**
- 수집 경로 **5개** 실연결: 업비트·바이낸스(24시간), KIS 웹소켓 틱, KRX 광역 스냅샷, 지수·금리
- 자산군 **4축**: 주식 · 암호화폐 · 지수 · 금리
- 구독자 데이터 무결성 **48.5% → 100.0000%**
- 배포 프로토콜 3종: TCP 바이너리 · WebSocket · REST

**L** — 피드 서비스의 어려움은 "받는 것"이 아니라 **"잃지 않는 것, 잃었을 때 아는 것"**
입니다. 무결성을 측정하지 않았다면 절반이 사라지는 것을 몰랐을 것입니다.

`src/mdfeed/services/` · `src/mdfeed/client.py` · README "무엇을 풀려고 만들었나"

---

## 필수 2. Linux 환경 기반 서비스·프로세스 점검 및 안정화

**S** — systemd 의 `Restart=on-failure` 는 **프로세스가 죽어야** 동작합니다.
그런데 피드 장애의 대부분은 프로세스가 멀쩡한 채로 옵니다 — TCP half-open,
거래소의 조용한 구독 해지, 이벤트 루프가 블로킹 호출에 물림.

**T** — 유닛 파일을 "썼다" 와 "돌려봤다" 를 구분해야 했습니다. 개발은 macOS 에서
했으므로 실제 systemd 아래에서 검증할 방법이 필요했습니다.

**A**
- *(판단)* liveness(`/healthz`) 와 readiness(`/readyz`) 를 나눴습니다. 기동 직후 DB
  연결 전 상태를 "죽었다" 로 판정해 재시작하면 영원히 못 뜹니다.
- *(판단)* systemd 가 못 잡는 장애를 위해 헬스 기반 워치독을 따로 뒀습니다.
  시간당 재시작 한도를 넘으면 "재시작으로 못 고치는 문제" 로 보고 사람을 호출합니다.
- *(구축)* systemd 가 실제로 도는 Ubuntu 22.04 테스트베드를 만들어 11개 항목을
  자동 검증하게 했습니다 (`make systemd-test`).
- *(발견)* 그 검증에서 **`Requires=` 가 소비자까지 연쇄 재시작**시키는 것을 찾았습니다.
  버스 구독자에 지수 백오프 자동 재접속을 넣어둔 이유가 무력화되고 있었습니다.
  `Wants=` + `After=` 로 바꿔 순서만 보장하게 했습니다.
- *(검증)* 복구 경로는 평소에 안 돌아갑니다 — 61분을 돌려도 재접속 0회였습니다.
  그래서 장애를 일부러 주입하는 하네스를 만들었습니다 (`make chaos`).

**R** — 실제 리눅스에서 확인한 것

| 항목 | 결과 |
|---|---|
| 6개 서비스 + target 기동 | 전부 `active (running)`, `User=mdfeed` |
| 보안 강화 | `ProtectSystem=strict` · `NoNewPrivileges` · `LimitNOFILE=65536` |
| SIGKILL 후 재시작 | feedd PID 111 → 170 |
| **소비자 생존** | writer PID 114 유지, 백오프 1s→2s→4s 후 자동 재접속 |
| 우아한 종료 | 잔여 버퍼 flush (3,083 → 3,109행) |

장애 주입 4종

| 시나리오 | 결과 |
|---|---|
| TCP 스트림 오염 | 3회 주입 → 재동기화 4회 후 **1,101건 계속 수신** |
| 읽지 않는 구독자 25초 | 게이트웨이 정상 유지 |
| DB 권한 박탈 | writer 생존, 복구 후 재개 (32,429 → 35,271) |
| feedd SIGKILL | writer PID 유지 + 재개 (38,047 → 39,975) |

**L** — **안 돌아본 복구 경로는 복구 경로가 아닙니다.** 그리고 설정 한 줄
(`Requires=`)이 설계 의도를 통째로 무력화할 수 있습니다.

`ops/systemd/` · `ops/ops.sh` · `ops/watchdog.sh` · `ops/chaos.sh` · `ops/testbed/`

---

## 필수 3. 데이터 파이프라인·배포·점검 등 운영 자동화

**S** — 수집기·게이트웨이·적재기가 여러 프로세스로 나뉘어 있고, 로컬·컨테이너·
리눅스 서버 세 환경에서 같은 방식으로 떠야 했습니다.

**T** — 사람이 외워야 할 명령을 줄이고, CI 가 사람과 똑같은 명령을 쓰게 만드는 것.

**A**
- Makefile 하나로 기동·점검·시험·배포를 통일했습니다. CI 도 같은 타깃을 부릅니다.
- GitHub Actions 6잡: 테스트(3버전) · 스택 스모크 · 벤치마크 · 컨테이너 빌드
- *(판단)* CI 는 거래소에 붙을 수 없으므로(불안정·지역 제한) **녹화 리플레이**로
  전 구간을 결정론적으로 검증하게 했습니다.
- *(판단)* CI 스모크가 참조 클라이언트로 실제 구독해 **무결성 100% 미달이면 빌드를
  실패**시킵니다. 배포단 회귀의 최후 방어선입니다.
- Pages 워크플로가 벤치마크·백테스트·품질 스냅샷을 **매번 새로 만들어** 대시보드에
  넣습니다. 사람이 수치를 옮겨 적지 않으므로 문서와 실제가 갈라지지 않습니다.

**R**
- `make demo` 한 줄로 7개 프로세스 기동 (설치할 패키지 없음)
- CI 6잡 · 테스트 **179개** · 매 푸시마다 통과
- 컨테이너 7개 전부 healthy · systemd 자동 검증 11항목
- 대시보드 지표 자동 갱신 (일 1회 + 푸시마다)

**L** — 자동화의 목적은 손을 덜 쓰는 것이 아니라 **사람과 CI 가 같은 것을 실행하게
만드는 것**입니다. 다르면 "내 로컬에선 되는데" 가 나옵니다.

`Makefile` · `.github/workflows/` · `docker-compose.yml`

---

## 필수 4. 언어 — Python

**S/T** — 수집·배포의 hot path 와 운영 도구 전부.

**A** — 코드 **8,835줄**(소스) + **1,879줄**(테스트) + **1,232줄**(운영 스크립트).
asyncio 기반 다중 프로세스, 표준 라이브러리 중심.

**R** — 핵심 경로 외부 의존성 **0**. `python3` 만 있으면 어떤 리눅스에서도 돕니다.
선택 의존성(psycopg2·pyzmq·pandas)은 없으면 각각 SQLite·UDS·자체 엔진으로 폴백합니다.

**L** — 의존성을 줄이면 배포가 쉬워지지만, **어디까지 직접 만들지 경계를 정하는 것**이
더 중요합니다. 백테스트는 `backtesting.py` 에 맡겼습니다.

---

## 필수 5. DB — SQL 기초, 관계형 DB 이해

**S** — 틱은 가장 크게 자라는 테이블이고, 조회 패턴은 "특정 종목의 특정 기간" 입니다.

**T** — 로컬·CI 는 설치 없이 돌면서 운영은 제대로 된 시계열 DB 를 쓰게 하는 것.

**A**
- *(설계)* `(venue, symbol, ts DESC)` 복합 인덱스. 단일 컬럼 3개보다 낫고
  최신 조회를 인덱스만으로 끝냅니다.
- *(설계)* 사전 집계 — 틱을 그대로 조회하면 매번 수백만 행을 스캔합니다.
  적재 시점에 1분봉으로 접고, upsert 로 `high=MAX·low=MIN·volume=SUM` 병합합니다.
  writer 재시작이나 버킷 경계 배치에서 실제로 필요합니다.
- *(설계)* TimescaleDB 하이퍼테이블 + 보존정책 (틱 30일 / 호가 7일)
- *(판단)* DB 연결 실패 시 SQLite 폴백. DB 가 안 뜬다고 수집까지 멈추면 그날
  데이터를 통째로 잃습니다.
- *(발견)* 컨테이너에서 `DATABASE_URL` 을 줬는데 **조용히 SQLite 로 동작**하고
  있었습니다. 이미지에 드라이버가 없었고, 폴백이 설정 오류와 DB 장애를 구분하지
  않았습니다. `ImportError` 를 따로 잡아 조치 방법과 함께 로그를 남기게 했습니다.
- *(발견)* Postgres 조회 시각이 UTC 라 국내 장 09:00~15:30 이 00:00~06:30 으로
  보였습니다. 스키마에서 DB 기본 시간대를 `Asia/Seoul` 로 설정했습니다.

**R**
- 배치 적재 **480,586 rows/s** (500건 또는 2초 중 먼저)
- 하이퍼테이블 3개 · 보존정책 2개 · 분석 뷰 4종
- SQLite·Postgres 동시 적재로 다중 소비자 검증 (시퀀스 갭 0, DB 오류 0)

**L** — 폴백은 좋은 장치지만 **설정 오류를 가리면 안 됩니다.** 조용히 다른 경로로
가는 것이 실패보다 나쁠 때가 있습니다.

`src/mdfeed/storage/schema.sql` · `storage/db.py`

---

## 필수 6. Linux — 명령·프로세스·로그 확인

**S** — 새벽에 호출받은 사람이 문서만 보고 1차 조치를 끝낼 수 있어야 합니다.

**A**
- `ops/ops.sh` — 프로세스 생존 · HTTP 헬스 · 포트 · UDS 소켓 · fd 수 · 구독자 연결을
  한 화면에. `diag` 는 업스트림·시퀀스 무결성·시계 동기화·디스크까지 한 번에 훑습니다.
- *(판단)* bash 3.2 호환으로 작성했습니다. 연관배열(`declare -A`)은 bash 4+ 전용이라
  macOS 기본 셸에서 깨집니다. 개발 노트북과 리눅스 서버에서 같은 스크립트를 써야 합니다.
- JSON 구조화 로그 → journald. `SIGHUP` 으로 로그 레벨 재적용 (logrotate 연동).
- `RUNBOOK.md` — 장애 시나리오 8종의 증상 → 진단 명령 → 조치 절차.

**R** — `./ops/ops.sh diag` 하나로 1차 진단 완료. 외부 감시용 헬스체크는
Nagios 규약 종료코드(0/1/2)를 냅니다.

**L** — 운영 문서는 **명령이 그대로 붙여넣기 가능해야** 쓸모가 있습니다.

`ops/ops.sh` · `RUNBOOK.md` · `ops/healthcheck.py`

---

## 필수 7. 프로세스 간 통신, 네트워크 프로토콜 (TCP/HTTP)

**S** — TCP 는 바이트 스트림이라 **메시지 경계가 없습니다.** `recv()` 한 번이
메시지 1.5개를 줄 수도, 3개를 줄 수도 있습니다. 상용 마켓데이터 피드가 공통으로
푸는 문제가 정확히 이것입니다.

**T** — 라이브러리를 써도 프레이밍·시퀀싱·갭 복구·스냅샷 복구는 어차피 직접 해야
합니다. 그 경계를 알려면 한 번은 아래까지 내려가 봐야 한다고 판단했습니다.

**A**

| 구현물 | 다룬 것 |
|---|---|
| **MDFP/1 TCP 프로토콜** | 길이 프리픽스 프레이밍, CRC32, 시퀀스 갭 탐지, 재동기화 |
| **WebSocket 클라이언트/서버** | RFC 6455 — Upgrade 핸드셰이크, `Sec-WebSocket-Accept`, 마스킹, 확장 길이(7/16/64bit), 단편화 재조립 |
| **HTTP/1.1 서버** | keep-alive, Content-Length 바이트 정확도, HEAD, 경로 탈출 차단 |
| **UDS pub/sub 버스** | 프로세스 간 팬아웃, 구독자별 백프레셔 |
| **공유메모리 링버퍼** | SPSC 브로드캐스트, 논블로킹 생산자, 찢긴 읽기 방지 |

- *(판단)* CRC32 를 넣었습니다. TCP 체크섬은 16비트라 약하고, 무엇보다
  **우리 쪽 프레이밍 버그를 못 잡습니다.** 오프셋이 하나 밀리면 TCP 는 통과시키고
  우리는 조용히 틀린 가격을 배포합니다.
- *(판단)* 백프레셔는 drop-oldest 입니다. 발행 경로에서 `drain()` 을 기다리면
  구독자 하나가 느려질 때 전체가 멈춥니다(head-of-line blocking).

**R**

| 계층 | 실측 |
|---|---|
| MDFP 인코딩 | 2,305,570 msg/s (434 ns) |
| MDFP 파싱 (재동기화 포함) | 759,786 msg/s |
| 공유메모리 링버퍼 | 1,560,551 msg/s |
| **UDS 순수 IPC 지연** | **p50 47.3 µs / p99 105.9 µs** |
| HTTP/1.1 (keep-alive) | 15,237 req/s |
| 전체 파이프라인 | 49,235 msg/s |

RFC 6455 명세 벡터 검증: `SHA1(key+GUID)` → `s3pPLMBiTxaQ9kYGzzhZRbK+xOo=` 일치.

**L** — 라이브러리가 무엇을 해주고 무엇을 안 해주는지는 **한 번 내려가 봐야** 압니다.
그 과정에서 문서가 틀린 곳 2군데, 측정이 거짓말하던 곳 2군데를 찾았습니다.

`src/mdfeed/protocol.py` · `wsproto.py` · `httpd.py` · `bus.py` · `ringbuffer.py`

---

## 필수 8. IT Knowhow — 자료구조·운영체제·네트워크

| 분야 | 이 프로젝트에서 |
|---|---|
| **자료구조** | 공유메모리 SPSC 링버퍼(찢긴 읽기 방지) · 로그버킷 히스토그램(고정 메모리 O(1)) · 증분 지표(O(1) 갱신) |
| **운영체제** | 공유메모리(`multiprocessing.shared_memory`) · 시그널(SIGTERM/SIGHUP) · 프로세스 감독 · 파일 디스크립터 관리 · systemd 보안 강화 |
| **네트워크** | TCP 프레이밍 · `TCP_NODELAY` · half-open 탐지 · 지수 백오프 + 지터 · AIMD 유량 제어 |

**실제로 겪은 OS 문제 두 가지**

1. **`asyncio.to_thread` 는 await 를 취소해도 스레드가 안 멈춥니다.** 종료 시
   태스크를 cancel 하고 DB 커넥션을 닫았더니, 아직 `executemany` 를 돌던 워커
   스레드 밑에서 객체가 해제돼 **use-after-free 로 프로세스가 죽었습니다.**
   DB 접근 경로 전체를 단일 락으로 직렬화해 해결했습니다.

2. **Python 3.12+ 의 `Server.wait_closed()` 는 핸들러 종료를 기다립니다.**
   핸들러는 큐에서 영원히 대기하므로 **SIGTERM 을 받고도 프로세스가 안 내려갔습니다.**
   리스너를 닫기 전에 연결 핸들러부터 취소하도록 고쳤습니다.

**AIMD 유량 제어** — KIS REST 한도가 문서와 달라 고정 3건/초로 두니 60초에 15번
거절당했습니다. TCP 혼잡제어와 같은 방식(거절 시 ×1.15 감속, 8회 연속 성공 시 ×0.9)
으로 바꿔 **2.0 req/s 로 수렴**했습니다.

**L** — 언어 런타임과 OS 의 경계에서 나는 문제는 **오래 돌려봐야만** 드러납니다.

---

## 우대 1. 데이터 파이프라인·인프라·자동화 (스크립트·배치·CI)

**S** — 이 프로젝트 전에 **SEC EDGAR·OpenDART 재무데이터 수집기를 2026년 4월부터
운영**해 왔습니다.

**A**
- XBRL 태그 다중 매핑 (매출이 `Revenues` 로도 `RevenueFromContract…` 로도 옴)
- `pending·done·error` 상태로 중단 후 재개 — 프로세스가 죽어도 done 은 건너뜁니다
- SEC 초당 10건 제한 대응(0.12초 대기 + User-Agent), OpenDART 0.08초
- 스케줄러 등록 → 부팅 시 재개, 매일 새벽 3시 CSV 내보내기·커밋·푸시
- 실행 상태·진행률·저장 건수를 한 번에 보는 점검 스크립트

**R** — 대상 10,158곳 전부 처리 → **6,890곳에서 487,434건**. 상태 테이블에
`error` 로 남은 건 없습니다. 저장소는 **143일 동안 146번** 자동 갱신됐습니다.
<https://github.com/oyeong011/financial-database>

**L** — 파이프라인의 핵심은 수집 속도가 아니라 **중단과 재개를 전제로 설계하는 것**
입니다. 이 저장소가 MDFeed 의 참조 데이터 평면이 됐습니다
(조인 키: `DART stock_code` = `KIS 종목코드`).

---

## 우대 2. Feed 기반 금융DATA 시세·마켓데이터 서비스 경험

**S** — 한국투자증권 실시간 API 를 신청해 실계좌로 연결했습니다.

**T** — 명세대로 구현했는데 **매도 체결이 전부 방향 미상**으로 떨어졌습니다.

**A** — 추측을 멈추고 재기로 했습니다. 체결가를 같은 메시지의 호가와 대조하면
방향을 알 수 있습니다 — 매도호가 이상에서 체결됐다면 공격적 매수입니다.

| 체결구분 | 매도호가 이상 | 매수호가 이하 | 판정 |
|---:|---:|---:|---|
| `1` | 70 | 3 | 매수 |
| `5` | 0 | 173 | **매도** |

문서는 `3` 이 매도라고 했지만 **`3` 은 한 건도 오지 않았습니다.**
매핑을 관측값 기준으로 고치고, 매핑에 없는 코드는 버리지 않고 세어
`/healthz` 에 노출했습니다. 거래소가 코드를 바꾸면 그 카운터가 먼저 알려줍니다.

같은 방식으로 구독 한도도 실측했습니다 — 문서는 41종목이라고 했지만 새 연결에서
한 종목씩 등록하니 **4번째부터 거절**당했습니다. 계정마다 다른 값이라 고정 상수를
믿지 않고, 거절 시점의 성공 개수를 실효 한도로 학습하게 했습니다.

**웹소켓으로 코스피 전체는 구조적으로 불가능**하므로 계층형 피드로 우회했습니다.

| 계층 | 방식 | 종목 | 갱신 |
|---|---|---:|---|
| 틱 | WebSocket | 3 | 밀리초 (실제 체결) |
| 활성 | 순위 API (30종목/요청) | ~60 | 10초 |
| 전체 | 유니버스 라운드로빈 | **1,783** | 15분 회전 |

**스냅샷을 체결로 위조하지 않았습니다.** 누적거래량 증가분만 합성 체결로 발행하고
venue 를 `KRX` 로 분리해 웹소켓의 진짜 체결(`KIS`)과 섞이지 않게 했습니다.

**R**
- 코스피 **1,783종목** 커버, 공개 대시보드에 1,153종목 표시
- 자산군 4축 (주식·암호화폐·지수·금리)
- **암묵 환율로 자체 정합성 검사** — 업비트 원화가 ÷ 바이낸스 달러가.
  BTC 1,386.7 · ETH 1,386.2 · SOL 1,386.0 KRW/USD, **괴리 0.050%**.
  외부 환율 소스 없이 세 종목이 서로를 검증합니다.
- 품질 검사 108,156건 → CRITICAL 0, WARNING 92 (전부 실제 시장 움직임)

**L** — **명세를 그대로 옮기기 전에 실제 데이터로 한 번 대조해야 합니다.**
값이 안 나오는 것은 금방 알아채지만, 조용히 절반이 사라지는 것은 알아채기 어렵습니다.

`src/mdfeed/adapters/kis*.py` · `src/mdfeed/quality.py`

---

## 필수 9. 해외여행 결격사유

병역 및 해외여행 결격사유 관련 사항은 지원서에 별도 기재합니다.

---

# 요건 대응 요약

| 요건 | 대표 근거 | 실측 |
|---|---|---|
| 금융 데이터 FEED 개발·운영 | 수집 경로 5개, 배포 프로토콜 3종 | 무결성 48.5% → **100.0000%** |
| Linux 서비스·프로세스 점검·안정화 | systemd 6유닛 + 자동 검증 + 장애 주입 | 11항목 · 복구 4종 확인 |
| 파이프라인·배포·점검 자동화 | Makefile · CI 6잡 · Pages 자동 갱신 | 테스트 **179개** |
| Python | 소스 8,835줄 | 핵심 의존성 **0** |
| SQL · 관계형 DB | 복합 인덱스 · 사전 집계 · 하이퍼테이블 | 적재 **480,586 rows/s** |
| Linux 명령·프로세스·로그 | `ops.sh diag` · RUNBOOK 8종 | 1차 진단 한 줄 |
| IPC · TCP/HTTP | 프로토콜 5종 직접 구현 | IPC 지연 **p50 47.3 µs** |
| 자료구조·OS·네트워크 | 링버퍼 · 시그널 · AIMD | 세그폴트·종료정지 실사례 |
| *(우대)* 파이프라인·CI | financial-database **143일 운영** | **487,434건**, error 0 |
| *(우대)* 마켓데이터 서비스 | KIS 실계좌 · 계층형 피드 | 코스피 **1,783종목** |

**개발 중 실측으로 잡아 기록한 결함 26건** — 원인과 조치를 전부 코드 주석과
문서에 남겼습니다. 이 목록이 이 프로젝트에서 가장 값진 부분입니다.
