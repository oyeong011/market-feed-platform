# 설계 문서

무엇을 왜 그렇게 만들었는지, 그리고 **어떤 대안을 왜 버렸는지** 적습니다.
코드만 보면 알 수 없는 판단의 근거가 이 문서의 존재 이유입니다.

---

## 1. 왜 수집을 한 프로세스로 몰았나

거래소 세션은 비쌉니다. 붙는 데 시간이 걸리고, 끊기면 그 구간이 갭이 되고,
갭은 복구 비용입니다. 배포·적재·전략이 각자 거래소에 붙으면 같은 데이터를 N번 받고
N배로 끊깁니다. 거래소의 연결 수 제한에도 걸립니다.

그래서 **수집은 `feedd` 하나**로 두고, 팬아웃은 로컬 IPC로 합니다.
상용 마켓데이터 시스템이 feed handler와 distributor를 나누는 이유와 같습니다.

반대급부는 `feedd`가 단일 장애점이라는 것입니다. 이를 다음으로 완화합니다.

- 어댑터별 독립 감독 루프 — 하나가 죽어도 나머지 업스트림은 계속 흐름
- 소비자 프로세스는 버스가 끊겨도 지수 백오프로 자동 재접속
- `feedd`가 죽어도 소비자는 살아 있으므로, 재기동 시 즉시 스냅샷부터 다시 받음
- systemd `Restart=on-failure` + 워치독의 헬스 기반 재시작

수평 확장이 필요해지면 venue별로 `feedd`를 쪼갭니다. 심볼 단위로 쪼개는 것보다
세션 경계와 일치해 운영이 단순합니다.

---

## 2. MDFP/1 프로토콜

### 2.1 왜 직접 만들었나

TCP는 바이트 스트림이라 **메시지 경계가 없습니다**. `recv()` 한 번이 메시지 1.5개를
줄 수도, 3개를 줄 수도 있습니다. 상용 마켓데이터 피드(FIX/FAST, NASDAQ ITCH,
인포맥스 자체 프로토콜)가 공통으로 푸는 문제가 정확히 이것입니다.

기성 직렬화(protobuf, msgpack)를 쓰면 이 문제가 **가려집니다**. 프레이밍·시퀀싱·
갭 복구는 여전히 직접 해야 하는데, 직렬화만 라이브러리에 맡기면 배우는 게 없습니다.

### 2.2 프레임 레이아웃

```
 +0   magic    4B   "MDF1"    스트림 재동기화 앵커
 +4   version  1B   = 1
 +5   msg_type 1B   HEARTBEAT/TRADE/BOOK/SIGNAL/SNAPSHOT/SUBSCRIBE
 +6   flags    2B   bit0 스냅샷, bit1 압축(예약)
 +8   seq      8B   채널 단위 단조증가
 +16  length   4B   페이로드 바이트 수
 +20  payload  NB
      crc32    4B   헤더+페이로드 CRC32
```

헤더 20B + CRC 4B = 24B 오버헤드. 체결 페이로드가 64B이므로 프레임당 88B,
오버헤드 37.5%입니다.

### 2.3 CRC32를 넣은 이유

TCP 체크섬은 16비트라 약하고, **무엇보다 우리 쪽 프레이밍 버그를 잡지 못합니다**.
오프셋이 하나 밀리면 TCP는 아무 이상 없이 통과시키고, 우리는 조용히 잘못된 가격을
전 구독자에게 배포합니다. 시세 서비스에서 이보다 나쁜 실패는 없습니다.

CRC 불일치를 만나면 다음 `MAGIC` 경계까지 건너뛰며 재동기화합니다.

> **실제로 여기서 버그가 있었습니다.** 재동기화 직후 파서가 남은 버퍼를 더 읽지 않고
> 반환하도록 짜여 있어서, 한 번 깨지면 세션 전체가 영구히 멎었습니다.
> 지금은 재동기화 후 루프를 계속 돌아 **오염된 프레임 1개만 버리고 나머지를 복원**합니다.
> `tests/test_protocol.py::test_crc_detects_corruption_and_recovers` 가 이를 고정합니다.

### 2.4 고정폭 페이로드

```python
Trade   "!16s8sQQddB7x"   64B   심볼16 · venue8 · 체결시각8 · 수신시각8 · 가격8 · 수량8 · 방향1 · 패딩7
BookTop "!16s8sQQdddd"    72B
Signal  "!16s8s16sQbdd7x" 72B
```

`Trade`를 64B(캐시라인)에 맞춘 것은 의도적입니다. 공유메모리 링버퍼 슬롯에 그대로
얹히고, 파싱에 분기가 없어 hot path 비용이 예측 가능합니다.

심볼이 16자를 넘으면 **자릅니다**. 옆 필드를 침범하지 않는 것이 우선이고,
정규화 심볼이 16자를 넘는 경우는 이 프로젝트 범위에서 없습니다.
(`tests/test_models.py::test_symbol_longer_than_16_is_truncated_not_corrupting`)

### 2.5 시퀀스 번호 — 설계를 한 번 틀렸다가 고친 부분

처음엔 발행자의 **전역 seq**를 그대로 흘렸습니다. 참조 클라이언트로 2개 심볼만
구독해 실측했더니 **데이터 무결성 48.5%** 가 나왔습니다. 한 건도 잃지 않았는데도요.

원인은 명확했습니다. 게이트웨이가 심볼 필터링을 하므로 구독하지 않은 심볼의 seq는
애초에 도달하지 않는데, 구독자는 그걸 유실로 셉니다. **필터링과 전역 시퀀싱은 양립하지 않습니다.**

지금은 게이트웨이가 **구독자별로 연속된 seq를 다시 매겨** 내보냅니다. 결과는 100.0000%.

- **비용**: 구독자 수만큼 프레임을 재인코딩합니다. CRC가 본문 전체를 덮으므로
  헤더만 갈아끼울 수 없습니다.
- **확장 경로**: 구독자가 수백 명 규모가 되면 심볼 그룹을 **고정 채널**로 묶고
  채널 단위 seq를 쓰면 됩니다(거래소 멀티캐스트 피드의 표준). 그러면 같은 채널
  구독자끼리 인코딩 결과를 공유할 수 있습니다. 현재 규모에서는 측정된 오버헤드가
  무시할 수준이라 단순한 쪽을 택했습니다.

### 2.6 스냅샷 + 증분

새 구독자는 과거 틱을 받지 못해 처음엔 아무 화면도 그리지 못합니다. 거래가 뜸한
종목은 몇 분씩 걸립니다. 그래서 접속 즉시 최신값 스냅샷을 `FLAG_SNAPSHOT` 과 함께
보내고, 마지막에 `next_seq` 를 담은 `MSG_SNAPSHOT` 으로 "증분은 여기서부터"를 알립니다.
이 값이 없으면 구독자는 첫 증분 프레임을 갭으로 오인합니다.

---

## 3. 프로세스 간 통신

### 3.1 왜 UDS인가 (그리고 왜 ZeroMQ가 기본이 아닌가)

| 후보 | 판단 |
|---|---|
| `multiprocessing.Queue` | pickle + 내부 락 + 피더 스레드. 틱당 수십 µs. **탈락** |
| TCP 루프백 | 동작하지만 체크섬·윈도우 계산 비용이 있고 포트를 소모 |
| **Unix Domain Socket** | 같은 호스트에서 가장 빠르고, 파일시스템 권한으로 접근제어. 의존성 0. **채택** |
| ZeroMQ | 여러 호스트로 흩어질 때 정답. 선택 의존성으로 둠(`MDFEED_BUS_BACKEND=zmq`) |

실측: 순수 IPC 지연 p50 **47.3 µs**, p99 105.9 µs, 처리량 상한 97,562 msg/s.

**제약**: `sockaddr_un.sun_path` 는 리눅스 108B, macOS 104B로 고정입니다.
넘으면 `bind`/`connect` 가 실패하는데, 재접속 루프 안에서 나면 원인 찾기가 오래 걸립니다.
기동 시점에 검사해 **조치 방법과 함께 즉시 실패**하게 했습니다.

### 3.2 백프레셔: 왜 drop-oldest인가

구독자마다 유한 큐를 두고, 차면 **가장 오래된 프레임부터 버리고 카운트**합니다.

발행 경로에서 `await writer.drain()` 을 직접 기다리면 구독자 하나가 느려질 때
전체 배포가 멈춥니다(head-of-line blocking). 그 순간 **모든** 구독자가 데이터를 못 받고,
업스트림 수신 버퍼도 차기 시작합니다.

시세 데이터는 오래된 값의 가치가 빠르게 떨어집니다. 오래된 틱을 지키자고 최신 틱
수신을 막는 건 본말전도입니다. **유실은 허용하되 반드시 셉니다.**
드롭이 임계치(5,000건)를 넘은 구독자는 끊어서 재접속·재동기화하게 합니다 —
연결을 유지한 채 조용히 틀린 데이터를 주는 것보다 안전합니다.

### 3.3 공유메모리 링버퍼

저지연 소비자를 위한 별도 경로입니다. 실측 push **1,560,551 msg/s** (641 ns).

**정책: 생산자는 절대 블로킹하지 않습니다.** 소비자가 느리면 생산자가 덮어쓰고
지나가고(lap), 소비자는 seq 비교로 "몇 건 건너뛰었다"를 감지합니다.

**찢긴 읽기(torn read) 방지**: 락 없이 읽으므로 복사 도중 생산자가 같은 슬롯을
덮어쓸 수 있습니다. 슬롯 앞뒤에 같은 seq를 적어두고(`seq_head`/`seq_tail`)
복사 후 일치를 확인합니다. 불일치면 버립니다 — Disruptor 계열의 고전적 기법입니다.

---

## 4. 시각과 지연시간

### 4.1 문제

지연시간을 `수신시각(우리 시계) − 체결시각(거래소 시계)` 로 재면, **두 시계의 오차가
그대로 섞입니다**. 바이낸스에 붙였을 때 실제로 **−13.5 ms**, 나중에는 **−30.8 ms**가
나왔습니다. 음수 지연은 물리적으로 불가능하니 로컬 시계가 그만큼 뒤처져 있다는 뜻입니다.

이 상태로 p99를 보고하면 전부 거짓말이고, 음수 지연이 든 DB 테이블은 어떤 SQL 집계를
해도 결론이 틀립니다.

### 4.2 해결: 최소값 필터 (NTP와 같은 아이디어)

```
관측값 = 진짜 편도지연 + 시계오프셋,   진짜 편도지연 ≥ 0
⟹ 충분한 표본의 최솟값 ≈ 시계오프셋
⟹ 보정지연 = 관측지연 − offset   (항상 ≥ 0, 상대 비교가 유효)
```

최소한 한 건은 큐잉 없이 거의 즉시 도착했다는 가정입니다.
**슬라이딩 윈도우**로 최솟값을 갱신하는 이유는 오프셋이 NTP 보정·서버 이전으로
시간에 따라 변하기 때문입니다. 고정 최솟값을 쓰면 한 번 튄 이상치에 영원히 묶입니다.

### 4.3 한계를 명시함

이 방법은 **편도 지연의 절대값을 주지 않습니다**. 왕복 측정 없이 편도를 정확히 아는
방법은 없고, 정확히 하려면 PTP 하드웨어 타임스탬프가 필요합니다.
여기서 얻는 것은 "기준선 대비 얼마나 튀었는가"이고, 운영 알람에는 충분합니다.

원시값(`ingest_latency_raw`)도 함께 기록합니다. **보정 로직이 틀렸을 때 원시값이 없으면
그 사실조차 알 수 없기 때문**입니다. 오프셋이 −1 ms를 넘으면
`/healthz` 에 "NTP 동기화 점검 필요" 경고를 띄웁니다.

---

## 5. 지표 계측

### 5.1 왜 평균이 아니라 분위수인가

마켓데이터에서 문제가 되는 건 "가끔 200 ms 튀는 구간"이고, 그건 평균에 묻힙니다.
실측 예: 정상 1 ms 구간에 2% 꼬리 500 ms를 섞으면 평균은 11 ms지만 p99는 500 ms입니다.

### 5.2 로그 버킷 히스토그램

전체 표본을 들고 있으면 메모리가 무한히 자랍니다. 데케이드당 20칸, 8데케이드
(1 µs ~ 100 s), 총 162칸 고정입니다. 버킷당 상대오차는 약 12%로 묶이고
기록은 O(1)입니다. HDR 히스토그램의 축소판입니다.

분위수 값이 실측 최대를 넘지 않도록 클램프합니다 — 로그 버킷은 위쪽으로
최대 12% 과대추정하는데, p99가 max보다 큰 리포트는 읽는 사람을 혼란스럽게 합니다.

### 5.3 분위수 해석 주의

꼬리 비율이 정확히 1%면 **p99는 경계에 걸려 본체 값을 냅니다.** 헷갈리기 쉬워
`tests/test_metrics_clock.py::test_exactly_1pct_tail_sits_at_p99_boundary` 로
동작을 명시적으로 고정해 뒀습니다.

---

## 6. 저장소

### 6.1 이중 백엔드

운영은 PostgreSQL + TimescaleDB가 맞습니다. 파티셔닝·보존정책·동시 조회가 필요합니다.
하지만 CI와 로컬 데모까지 Postgres를 요구하면 "일단 돌려보기"의 문턱이 높아집니다.

두 스키마의 컬럼 이름과 의미를 1:1로 맞춰 두었으므로 애플리케이션 쿼리는
플레이스홀더(`?` vs `%s`)만 바뀝니다.

`DATABASE_URL` 연결에 실패하면 **SQLite로 폴백**합니다. DB가 안 뜬다고 수집까지 멈추면
그날 데이터를 통째로 잃습니다. 로컬에 쌓아두고 나중에 이관하는 편이 낫습니다.

### 6.2 인덱스 설계

```sql
CREATE INDEX idx_trades_sym_ts ON trades (venue, symbol, ts DESC);
CREATE INDEX idx_trades_ts     ON trades (ts DESC);
```

조회 패턴은 사실상 "특정 종목의 특정 기간"입니다. `(venue, symbol, ts)` 복합 인덱스가
단일 컬럼 인덱스 3개보다 훨씬 낫고, `ts DESC` 로 최신 조회를 인덱스만으로 끝냅니다.

### 6.3 사전 집계

틱을 그대로 조회하면 대시보드·백테스트가 매번 수백만 행을 스캔합니다.
적재 시점에 1분봉으로 접어두는 편이 압도적으로 쌉니다.
Upsert는 `high=MAX`, `low=MIN`, `volume/tick_count=SUM` 으로 병합합니다 —
writer 재시작이나 버킷 경계에 걸친 배치에서 실제로 필요합니다.

### 6.4 배치 적재

틱마다 `INSERT` + `COMMIT` 하면 초당 수백 건에서 이미 fsync가 병목이고,
WAL fsync가 이벤트 루프를 막습니다. **500건 또는 2초 중 먼저 오는 쪽**에
`executemany` 로 밀어 넣습니다. 실측 480,586 rows/s.

호가는 체결보다 훨씬 자주 바뀌므로 **심볼당 1초 1건으로 샘플링**합니다.
스프레드 통계에는 그걸로 충분하고, 전부 적재하면 용량이 수십 배가 됩니다.

### 6.5 종료 시 flush — 그리고 여기서 세그폴트가 났다

SIGTERM을 받으면 버퍼에 남은 것을 반드시 쓰고 내려갑니다. 안 그러면 **정상 배포마다
최대 2초치 데이터가 사라집니다**.

그런데 여기서 프로세스가 죽었습니다.

> `asyncio.to_thread` 로 띄운 작업은 **await를 취소해도 스레드가 멈추지 않습니다.**
> 종료 시 태스크를 cancel하고 곧바로 커넥션을 닫으면, 아직 `executemany` 를 돌고 있는
> 워커 스레드 밑에서 sqlite3 객체가 해제돼 **use-after-free로 프로세스가 죽습니다.**
> 통합 테스트를 전체로 돌렸을 때 재현됐습니다.
>
> 조치: DB를 만지는 모든 경로(주기 flush, 배치 flush, 종료 close)가 하나의
> `threading.Lock` 을 통과하게 했습니다. 종료 시 close는 진행 중인 flush가 끝날 때까지
> 자연스럽게 대기합니다. `rest_api` 에도 같은 위험이 있어 동일하게 처리했습니다.

---

## 7. 서비스 종료

Python 3.12+ 의 `Server.wait_closed()` 는 **모든 연결 핸들러가 끝나야 반환합니다.**
그런데 핸들러는 `queue.get()` 이나 `reader.read()` 에서 대기 중이라 스스로 끝나지 않습니다.
결과: **SIGTERM을 받고도 프로세스가 안 내려가고, systemd가 `TimeoutStopSec` 뒤에 SIGKILL** —
그 순간 버퍼가 통째로 날아갑니다.

조치: 리스너를 닫기 **전에** 연결 핸들러부터 취소합니다.
`bus.UDSPublisher`, `httpd.HTTPServer`, `tcp_gateway` 세 곳 모두 적용했습니다.
`wait_closed()` 에는 5초 타임아웃도 걸어 최악의 경우에도 종료가 진행되게 했습니다.

CI가 이를 회귀 검사합니다 — SIGTERM 12초 후 `mdfeed.services` 프로세스가 남아 있으면 실패.

---

## 8. 리눅스 운영

### 8.1 liveness와 readiness를 나눈 이유

- `/healthz` (liveness) — 실패하면 **재시작해야 함**
- `/readyz` (readiness) — 실패하면 **트래픽에서 잠시 빼야 함**

기동 직후 DB 연결 전 상태를 "죽었다"고 판정해 재시작하면 영원히 못 뜹니다.
실제 운영에서 자주 나오는 재시작 루프의 원인입니다.

`feedd` 의 readiness는 "첫 틱을 실제로 받았는가"입니다. 포트가 열린 것과
데이터가 흐르는 것은 다릅니다.

### 8.2 systemd 재시작 정책

```ini
Restart=on-failure          # always 로 두면 수동 정지조차 되살아난다
StartLimitIntervalSec=60
StartLimitBurst=5           # 설정 오류로 무한 재시작하는 대신 실패 상태로 남긴다
TimeoutStopSec=30           # 버퍼 flush + DB 커밋에 필요한 시간
```

### 8.3 워치독이 따로 있는 이유

`Restart=on-failure` 는 **프로세스가 죽어야** 동작합니다. 그런데 피드 장애는 대부분
프로세스가 멀쩡한 채로 옵니다.

- TCP half-open — 소켓은 `ESTABLISHED` 인데 데이터가 안 옴
- 거래소가 구독을 조용히 해지 — 에러 없이 스트림만 멎음
- 이벤트 루프가 블로킹 호출에 물려 정지

전부 `/healthz` 로만 탐지됩니다. 그래서 **헬스 판정 → 재시작** 경로를 따로 뒀습니다.
시간당 재시작 한도(4회)를 넘으면 재시작으로 못 고치는 문제로 보고 알림을 띄우고 멈춥니다.

어댑터 자체에도 **정체(staleness) 감지**를 넣었습니다. 연결은 살아있는데
`stale_after_s` 동안 메시지가 없으면 스스로 끊고 다시 붙습니다.

### 8.4 재접속 백오프에 지터를 섞은 이유

여러 어댑터가 동시에 끊겼을 때(거래소 점검, 회선 장애) 재접속이 한 시점에 몰려
상대 서버를 때리는 thundering herd를 피합니다. `delay = min(backoff, 30) × (0.5 + random)`.

---

## 9. 브라우저 배포에서 틱을 병합하는 이유

초당 수백 틱을 그대로 밀면 브라우저 렌더링이 못 따라가 탭이 멈춥니다.
심볼당 최신값만 남겨 **100 ms 주기로 묶어** 보냅니다. 사람 눈은 초당 10프레임 이상을
구분하지 못하므로 정보 손실이 사실상 없습니다.

체결 로그가 필요한 구독자는 `?mode=raw` 로 원본 스트림을 받습니다.
시그널은 **병합하지 않습니다** — 하나하나가 사건입니다.

---

## 10. 백테스트의 정직성 장치

1. **미래 참조 차단** — `t` 봉 종가로 판단, **`t+1` 봉 시가**로 체결.
   같은 봉 종가로 체결하면 "종가를 보고 종가에 산" 것이 되어 성과가 부풀려집니다.
   백테스트가 실전보다 좋게 나오는 가장 흔한 원인입니다.
2. **수수료 + 슬리피지** — 편도 0.05% + 5 bp. 왕복 비용을 빼지 않은 수익률은 무의미합니다.
3. **미청산 포지션 강제 청산** — 남기면 최종 자산이 부풀려집니다.
4. **실시간과 같은 코드** — 전략 클래스를 공유합니다. 따로 구현하면 미묘하게 갈리고,
   그 차이는 실거래에서만 드러납니다.
5. **오픈소스 교차검증** — `backtesting.py`/`vectorbt`/`pandas` 로 다시 계산해 대조합니다.

> **비교는 사과 대 사과로 맞춰야 합니다.** 처음엔 우리 결과(슬리피지 5 bp 포함)를
> `backtesting.py`(슬리피지 없음)와 그냥 비교해 0.21%p 차이를 봤습니다. 그게 엔진 버그인지
> 가정 차이인지 알 수 없었습니다. 비교용으로 우리 엔진도 슬리피지 0으로 다시 돌리자
> **0.012%p** 로 줄었고, 남은 차이는 체결 규칙(부분청산 등)으로 설명됩니다.

**모델링하지 않는 것**: 부분 체결, 레버리지, 증거금, 호가 잔량 기반 시장충격.
못 하는 걸 한 척하느니 범위를 좁히고 그 사실을 적습니다.

---

## 11. 팩터 스크리닝에서 결측을 0으로 채우지 않는 이유

채우는 순간 **"부채가 없는 회사"와 "부채 데이터가 없는 회사"가 같아집니다.**
스크리닝 결과가 조용히 오염되는 전형적 경로입니다.

각 팩터를 백분위로 정규화해 합산합니다. 원값을 그대로 더하면 단위가 큰 팩터(매출액)가
점수를 독식합니다. 결측 종목은 그 팩터에서 중앙값(0.5)을 받아 과도한 불이익도 이득도
없게 합니다.

---

## 12. 테스트 전략

### 12.1 리플레이 데이터에 지연 지표를 매기지 않는 이유

리플레이는 녹화된 체결 시각을 그대로 흘립니다. 그 시각과 현재 시각의 차이를 "지연"으로
계산하면 **언제 녹화했는가**가 지연으로 둔갑합니다. CI에서 실제로
`시계 오프셋 2,343,288 ms` 경고가 떠 빌드가 실패했습니다.

어댑터에 `measures_latency` 플래그를 뒀습니다. 실시간 거래소는 `True`,
리플레이는 `False` 입니다. 헬스체크도 이 플래그를 보고 시계 검사 대상에서 제외합니다.

추가로, **실시간 배속(speed > 0)으로 재생할 때는 체결 시각을 현재 기준으로 평행이동**합니다.
안 그러면 몇 시간 전 타임스탬프로 봉이 만들어져 대시보드와 REST 조회에 과거 시각이 뜹니다.
평행이동이므로 틱 간격은 보존됩니다. 최대속도(speed = 0)에서는 재기준하지 않습니다 —
18분 데이터를 20초에 쏟아내므로 첫 프레임 기준으로 옮기면 뒷부분이 미래 시각이 되어
더 이상해집니다. 장애 재현에는 원본 시각이 필요하므로 `MDFEED_REPLAY_RESTAMP=0` 으로 끕니다.

### 12.2 CI 전략

**CI는 거래소에 붙을 수 없습니다** — 불안정하고, 지역 제한이 있고, 남의 서버입니다.
그래서 합성 틱으로 MDFP 녹화 파일을 만들고 리플레이 어댑터로 흘려보냅니다.
**실제 운영 경로와 같은 코드**가 돌기 때문에 파이프라인이 끊기면 여기서 잡힙니다.

E2E 테스트가 고정하는 것:

1. 수집기가 버스에 발행하고 게이트웨이가 TCP로 배포한다
2. 구독자가 받는 seq가 연속이다 (구독자별 재넘버링)
3. 스냅샷이 증분보다 먼저 온다
4. writer가 DB에 적재하고 1분봉 OHLC 관계가 성립한다
5. 전략 엔진이 봉을 닫는다
6. 종료 시 버퍼가 flush된다

CI 스모크 잡은 실제로 스택을 띄우고 참조 클라이언트로 구독해
**데이터 무결성 100% 미달이면 빌드를 실패**시킵니다. 배포단 회귀의 최후 방어선입니다.
SIGTERM 12초 뒤 프로세스가 남아 있어도 실패시킵니다 — §7의 종료 결함이 재발하는 걸 막습니다.

헬스체크는 **CRIT(exit 2)만 빌드를 실패**시킵니다. WARN 은 "저하됐지만 서비스 중"이고,
CI 러너의 시계 상태 같은 환경 요인으로도 뜰 수 있기 때문입니다.

---

## 13. 버린 대안들

| 대안 | 버린 이유 |
|---|---|
| `websockets` / `aiohttp` 라이브러리 | 프로토콜 이해를 증명하는 것이 목표. 부수 효과로 의존성 0 달성 |
| FastAPI + uvicorn | 같은 이유. 직접 구현한 HTTP 서버가 15,237 req/s로 이 용도에 충분 |
| protobuf / msgpack | 프레이밍·시퀀싱·갭 복구는 여전히 직접 해야 함. 직렬화만 맡기면 배우는 게 없음 |
| `multiprocessing.Queue` | pickle + 락 + 피더 스레드로 틱당 수십 µs |
| 가격을 정수 tick으로 | 거래소별 tick size 테이블이 필요. 이 범위에서는 과설계 |
| 전체 depth 오더북 | 복원·검증 비용이 크고 이 프로젝트가 증명하려는 것과 직교 |
| 단일 프로세스 all-in-one | 배포단 장애가 수집을 죽임. 자원 한계·재시작 정책을 따로 걸 수 없음 |
| pandas 기반 실시간 지표 | 틱마다 전체 시리즈 재계산 = O(N) × 틱 수. 증분 O(1)로 구현 |

---

## 14. 앞으로 한다면

우선순위 순입니다.

1. **채널 기반 시퀀싱** — 구독자별 재인코딩을 없애고 수백 구독자까지 확장
2. **전체 depth 오더북** — 스냅샷 + 증분 복원, 체크섬 검증
3. **KIS 어댑터 실계좌 검증** — 현재는 명세 기반 구현까지만
4. **PTP / 하드웨어 타임스탬프** — 편도 지연의 절대값 측정
5. **Prometheus + Grafana 대시보드** — `/metrics` 는 이미 노출 중
6. **멀티 호스트 배포** — ZeroMQ 버스 백엔드는 이미 인터페이스만 준비됨
