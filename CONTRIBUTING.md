# 개발 가이드

## 시작

```bash
make install    # venv + pytest
make test       # 109개 테스트 (네트워크 불필요)
make lint       # 문법·임포트·셸 문법 점검
```

## 원칙

1. **핵심 경로에 의존성을 추가하지 않습니다.** 수집→배포→적재는 표준 라이브러리만
   씁니다. 새 라이브러리가 필요하면 `pyproject.toml` 의 optional-dependencies 에 넣고
   **없을 때의 폴백 경로**를 함께 구현하세요.

2. **버그를 고치면 그 버그를 고정하는 테스트를 남깁니다.** 어떤 증상이었고 원인이
   무엇이었는지 테스트 docstring 에 적으세요. `tests/` 의 기존 테스트들이 예시입니다.

3. **설계 판단은 주석에 근거를 남깁니다.** "무엇을 하는가"는 코드가 말합니다.
   주석은 "왜 이렇게 했는가"와 "어떤 대안을 왜 버렸는가"를 적는 자리입니다.

4. **수치를 손으로 옮겨 적지 않습니다.** 벤치마크·백테스트 결과는 스크립트가 JSON 을
   만들고 문서와 대시보드가 그걸 읽습니다.

## 새 거래소 어댑터 추가

1. `src/mdfeed/adapters/<venue>.py` 에 `Adapter` 를 상속해 `session()` 구현
2. 원본 메시지를 `models.Trade` / `models.BookTop` 으로 정규화해 `self._mark(msg)` 호출
3. 자격증명이 필요하면 `enabled()` / `disabled_reason()` 을 구현 —
   **선택적 업스트림이 전체 서비스를 막으면 안 됩니다**
4. `adapters/__init__.py` 의 `REGISTRY` 에 한 줄 추가
5. 그 거래소의 프로토콜 함정을 모듈 docstring 에 적으세요
   (Upbit가 바이너리 opcode로 JSON을 보내는 것 같은)

## 테스트 작성

- **네트워크에 의존하지 마세요.** 리플레이 어댑터와 합성 데이터를 씁니다.
- UDS 소켓 경로는 짧게 (`tempfile.mkdtemp(dir="/tmp")`) — 104바이트 제한
- 포트는 테스트 전용 대역(19xxx)을 쓰세요
- 공유메모리 이름은 테스트마다 고유하게

## 커밋

무엇을 왜 바꿨는지 적으세요. 결함 수정이면 증상과 원인을 함께 적습니다.
